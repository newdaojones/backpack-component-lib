import React from 'react';
import { View } from 'react-native';

const BankCardView: React.FC = () => {
  return (
    <View>
      { /* Content here */}
    </View>
  );
};

export default BankCardView;
